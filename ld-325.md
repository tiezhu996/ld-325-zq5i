<!-- 改编自：农业与生活服务主题项目提示词/ld-325/ld-325.md；后端由 NestJS 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目概述

**建材比价平台** — 面向装修业主和施工队的建材价格对比平台，聚合多商家建材报价，提供价格趋势分析、收藏对比和降价提醒功能，帮助用户买到性价比最高的建材。

**项目类型：全栈Web应用**

## 项目主要需求

1. **建材分类与搜索**：系统建立完整的建材分类体系（瓷砖、地板、涂料、卫浴、五金、门窗、灯具、管材等），支持多级分类浏览和关键词搜索，每个建材展示缩略图、品牌、规格型号、单位等基本信息，搜索结果支持按价格、销量、评分排序。

2. **多商家报价录入**：支持多个商家入驻并录入自家建材报价，同一款建材可显示多个商家的不同价格，报价包含单价、起订量、运费说明、交货周期，商家信息包含店铺名称、地址、联系方式和评分。

3. **价格对比与收藏**：用户可将多款建材加入对比清单，并排展示各项参数和价格差异，高亮最低价选项，支持收藏感兴趣的建材到个人收藏夹，收藏列表支持分类管理和批量对比。

4. **价格趋势图**：为每款建材展示近 30 天/90 天/1 年的价格走势图，数据来源为商家报价更新记录，趋势图以折线图形式展示，支持查看最高价、最低价和均价统计，帮助用户判断购买时机。

5. **价格预警订阅**：用户可为特定建材设置目标价格，当任意商家的报价低于目标价时，系统通过站内消息通知用户，支持设置价格波动百分比预警（如降价 10% 时提醒）。

6. **供应商管理**：管理员可审核商家入驻申请，管理商家资质信息，支持对商家进行评分和评论，商家可管理自己的商品报价和库存状态（有货/缺货/停产）。

7. **装修预算计算器**：提供简易装修预算工具，用户选择房间类型和面积，系统根据市场均价估算各项建材所需数量和总费用，预算结果可保存和导出，与实际采购价格对比分析。

## 代码架构要求（强制执行）

- **单一职责原则**：每个功能模块必须拆分成独立的文件维护。例如：建材相关拆分为 `product.handler.go`、`product.service.go`、`product.repository.go`、`product.router.go`、`product.validator.go`、`price_history.service.go`；每个图表组件独立文件。
- **源码文件要多**：鼓励细粒度拆分，禁止一个文件超过 400 行。前端组件按功能拆分，每个页面拆分为多个子组件；后端按实体/功能拆分为 handler、service、repository、middleware、dto、router 等独立文件。
- **统一维护常量**：所有硬编码字符串、数字、枚举值必须抽取到 `internal/constants/` 目录下的独立文件中（如 `app_constants.go`、`error_codes.go`、`product_categories.go`、`price_thresholds.go`），禁止在业务代码中直接写魔法数字或字符串。
- **统一维护日志**：日志配置和日志工具类统一放在 `internal/logger/` 中（如 `logger.go`），所有模块通过同一日志实例记录，支持分级（debug/info/warn/error）。
- **统一维护异常信息**：自定义错误类型和错误码统一放在 `internal/errors/` 目录下（如 `business_error.go`、`validation_error.go`、`price_not_found_error.go`），错误码和错误消息集中管理，禁止在业务代码中直接返回裸字符串错误。

## 技术要求

- **英文短名（用于 Docker Compose）**：`cybuildprice`
- **前端端口**：`FRONTEND_PORT=18625`
- **后端端口**：`BACKEND_PORT=19625`
- **前端框架**：Next.js 14 + TypeScript
- **UI 库**：Tailwind CSS + shadcn/ui
- **图表库**：ECharts
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL
- **缓存**：Redis
- **前端构建工具**：Next.js 内置构建
- **后端构建工具**：go build / go mod tidy
- **认证方式**：JWT（使用 github.com/golang-jwt/jwt/v5，保留角色/RBAC 描述）
- **Redis 客户端**：github.com/redis/go-redis/v9

## 后端目录结构（Go 标准布局，强制执行）

后端代码放在 `backend/`，数据库脚本放在 `backend/database/`：

```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/       # 配置加载（环境变量、PostgreSQL/Redis/JWT）
│   ├── model/        # GORM 模型（建材、分类、商家、报价、收藏、价格预警、预算等）
│   ├── repository/   # 数据访问层
│   ├── service/      # 业务逻辑层
│   ├── handler/      # HTTP 处理函数（等价于 controller）
│   ├── router/       # Gin 路由注册
│   ├── middleware/   # JWT 鉴权、RBAC、请求日志、错误恢复等
│   ├── dto/          # 请求/响应结构体与参数校验
│   ├── constants/    # 常量、错误码、枚举值
│   ├── errors/       # 统一错误类型
│   └── logger/       # 统一日志
└── database/
    ├── migrations/   # 数据库迁移脚本
    └── seeds/        # 演示数据种子
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
数据库脚本放在 `backend/database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cybuildprice`，确保 Compose 项目名不依赖目录名（中文目录名会被规范化为空导致 `project name must not be empty` 错误）
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目 Go 多阶段构建镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃，会产生 `the attribute version is obsolete` 警告）
- 顶层声明 `name: cybuildprice` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name` 命名容器，便于管理；容器名建议带 `${COMPOSE_PROJECT_NAME:-cybuildprice}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（提供 `GET /healthz` 健康检查接口），前端 `depends_on` 后端健康
- 暴露端口：前端 `18625:80`，后端 `${BACKEND_PORT:-19625}:8080`，数据库使用标准端口
- 所有可变配置（端口、数据库账号密码、JWT 密钥等）通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME`：英文短名（小写字母/数字/`-`/`_`），如 `cybuildprice`
- 数据库连接相关：`DB_NAME` / `DB_USER` / `DB_PASSWORD`
- 后端：`JWT_SECRET` 或等价的密钥配置
- 端口映射：`FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`（可选，便于本地冲突时调整）

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

后端构建要求：
- 依赖安装使用 `go mod tidy`
- 本地运行使用 `go run ./cmd/server`
- 构建使用 `go build ./...`（单服务）或 `go build ./cmd/server`
- `go.mod` / `go.sum` 必须声明全部 Go 依赖
- 后端提供 `GET /healthz` 健康检查接口，返回 200

自检清单（生成代码后必须确认）：
1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`（不需要 `-p`）
5. 在 `backend/` 下执行 `go mod tidy`、`go build ./...` 均无错误

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 必须包含以下信息：
- **项目类型标签**：全栈Web应用
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端技术栈写 Go 1.22 + Gin + GORM
- 不要保留脚手架默认 README 内容

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: cybuildprice   # 顶层 name 字段兜底，避免中文目录名导致 project name 为空

services:
  db:
    image: postgres:16-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-cybuildprice}-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-cybuildprice}-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-cybuildprice}-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-19625}:8080"
    environment:
      APP_ENV: production
      SERVER_PORT: 8080
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-cybuildprice}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18625}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db_data:
  redis_data:
```

`.env.example` 参考：

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=cybuildprice

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd

JWT_SECRET=change_me_to_a_long_random_string

FRONTEND_PORT=18625
BACKEND_PORT=19625
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## backend/Dockerfile 参考（务必遵循）

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配（至少包含 `node_modules/`、`dist/`、`.next/`、`.env`、`.DS_Store`、`*.log`、`.idea/`、`.vscode/`，以及 Go 相关 `bin/`、`backend/server`、`*.exe`、`*.test`、`coverage.out`）。
